# AISmartMirror - Face detection + recognition pipeline
#
# Windows: Run scripts/install_deps.ps1 to install (avoids dlib build/CMake).
# Or manually:
#   pip install dlib-bin
#   pip install face-recognition --no-deps
#   pip install -r requirements-base.txt
#
# CNN pipeline: pip install -r requirements-cnn.txt
