# AISmartMirror - Face detection + recognition pipeline
# Camera, detection, recognition (local)

# Windows: install dlib-bin first (pre-built wheel), then face_recognition --no-deps
dlib-bin
opencv-python>=4.8.0
numpy>=1.24.0
mediapipe==0.10.13
face-recognition>=1.3.0
python-dotenv>=1.0.0

# Development
pytest>=7.4.0
