# AISmartMirror - Environment Variables
# Copy this file to .env and configure as needed.
# The app runs without any of these for the starter phase.

# Logging
LOG_LEVEL=INFO
DEBUG=false
# Set DEBUG_SAVE_FRAME=true to save first frame as debug_frame.jpg
DEBUG_SAVE_FRAME=false

# Camera (0 = default webcam)
CAMERA_INDEX=0
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
CAMERA_FPS=30
# Backend on Windows: DSHOW, MSMF, or empty for auto
CAMERA_BACKEND=

# Vision
# Confidence: 0.0-1.0. Lower = more sensitive (catches distant faces, more false positives)
FACE_DETECTION_CONFIDENCE=0.4
# Model: 0=short-range (2m), 1=full-range (5m). Use 1 for distant faces
FACE_DETECTION_MODEL=1
# Path to known faces (profiles.json + .npy embeddings). Empty = data/known_faces
KNOWN_FACES_DIR=

# Face recognition (known vs unknown)
# Threshold: max distance for match. Lower=stricter, higher=lenient (see README)
FACE_RECOGNITION_THRESHOLD=0.6
# Run recognition every N frames (1=every frame). Higher reduces lag
FACE_RECOGNITION_INTERVAL_FRAMES=5
# Require N consecutive matching results before confirming identity (reduces flicker)
RECOGNITION_CONFIRMATION_COUNT=2
# Show recognition debug info on screen (distance, threshold, state)
DEBUG_RECOGNITION=false

# CNN face recognizer (optional - separate from embedding baseline)
USE_CNN_RECOGNIZER=false
CNN_MODEL_DIR=
# Live CNN defaults (match scripts/test_cnn_live.py)
CNN_CONFIDENCE_THRESHOLD=0.62
CNN_MIN_CLASS_MARGIN=0.15
# Reject indecisive softmax (nats); uniform over 6 classes ~1.79; try 1.4 or leave empty
CNN_MAX_SOFTMAX_ENTROPY=

# Guided enrollment
ENROLLMENT_TOTAL_SAMPLES=24
ENROLLMENT_MIN_FACE_SIZE=72
ENROLLMENT_MIN_LAPLACIAN_VAR=45
ENROLL_CAPTURE_INTERVAL_FRAMES=2
ENROLL_CAPTURE_COOLDOWN_SECONDS=0.55
# Backward-compatible alias (same value as ENROLL_CAPTURE_COOLDOWN_SECONDS)
ENROLLMENT_CAPTURE_COOLDOWN_SEC=0.55
ENROLLMENT_DUPLICATE_DISTANCE=0.03
ENROLLMENT_STEADY_FRAMES_REQUIRED=3
# Default false for prototype reliability: use prompt-based quality capture
ENROLL_REQUIRE_POSE_MATCH=false
# Max seconds per target pose before auto-advance with warning
ENROLL_POSE_TIMEOUT_SEC=12

# Cloud APIs (optional - leave empty for now)
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
N8N_WEBHOOK_URL=
